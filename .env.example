# Server Configuration
PORT=3001

# Redis Caching Configuration
# Set to 'true' to enable Redis caching for faster lazy loading
# Set to 'false' or leave unset to disable Redis (default: disabled)
USE_REDIS=false

# Redis Connection (only used if USE_REDIS=true)
# REDIS_HOST=localhost
# REDIS_PORT=6379
